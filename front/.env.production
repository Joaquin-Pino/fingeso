# En producción la SPA normalmente no comparte origen con el backend Spring Boot,
# así que aquí hay que apuntar a la URL real de la API (y el backend deberá
# habilitar CORS para ese origen, algo pendiente y fuera del alcance del frontend).
VITE_API_BASE_URL=/api
